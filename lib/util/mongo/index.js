
const insert = require('./insert');
const find = require('./find');
const distinct = require('./distinct');

/**
 * Mongo namespace
 * @namespace Mongo
 */
module.exports = {
  insert,
  find,
  distinct
};
